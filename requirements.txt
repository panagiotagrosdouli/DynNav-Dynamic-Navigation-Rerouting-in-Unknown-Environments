# DynNav Streamlit dashboard — minimal runtime requirements.
# Designed to run on any machine with Python 3.10+ (no GPU, no ROS 2).
streamlit>=1.32
numpy>=1.24
pandas>=2.0
plotly>=5.18
matplotlib>=3.7
networkx>=3.1
